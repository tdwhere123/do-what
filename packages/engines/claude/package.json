{
  "name": "@do-what/claude",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start-mcp": "pnpm run build && node ./dist/mcp-server.js",
    "test": "pnpm --filter @do-what/protocol build && pnpm --filter @do-what/core build && pnpm run build && node ./dist/__tests__/run-tests.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@do-what/core": "workspace:*",
    "@do-what/protocol": "workspace:*"
  }
}
