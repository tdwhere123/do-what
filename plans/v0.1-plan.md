# v0.1 — 清理基线

**目标**：删除不用的功能，减少维护负担，保持最小可跑状态。

## 要删除的内容

### 自动更新模块
- [ ] 删除 `packages/app/src/app/context/updater.ts`
- [ ] 从 `app.tsx` 移除所有 `updateHandle` / `updateAutoCheck` 相关逻辑
- [ ] 从 `packages/app/package.json` 移除 `@tauri-apps/plugin-updater`
- [ ] 从 `packages/desktop/src-tauri/tauri.conf.json` 移除 updater plugin
- [ ] 从 `packages/desktop/src-tauri/Cargo.toml` 移除 updater feature

### OpenCode Router 模块
- [ ] 删除 `packages/opencode-router/`（整个包）
- [ ] 删除 `packages/app/src/app/pages/identities.tsx`
- [ ] 从 `packages/server/src/` 移除 `/opencode-router/*` 路由
- [ ] 从 `packages/app/src/app/lib/openwork-server.ts` 移除 Router 相关类型（`OpenworkOpenCodeRouter*` 系列）
- [ ] 从 `packages/desktop/src-tauri/tauri.conf.json` 移除 `sidecars/opencode-router`
- [ ] 从 `packages/app/src/app/types.ts` 中 `DashboardTab` 移除 `identities`
- [ ] 从 `dashboard.tsx` 移除 `identities` tab 相关代码

### Dashboard Tab 合并
- [ ] `plugins` 和 `mcp` 统一走 `extensions.tsx`，移除独立 tab 入口
- [ ] 将 `config` 的内容并入 `settings` 的新 `workspace` 子 tab
- [ ] 最终 `DashboardTab` 类型：`"scheduled" | "soul" | "skills" | "extensions" | "settings"`
- [ ] 从 `pnpm-workspace.yaml` 移除 `opencode-router` 包

## 验证标准
- `pnpm typecheck` 通过
- `pnpm dev:ui` 启动无报错
- Dashboard 只显示保留的 5 个 tab

## 注意事项
- `openwork-server.ts` 中有大量 Router 类型，逐一检查依赖，避免误删
- `app.tsx` 中 `updateHandle` 相关逻辑分散，需全文搜索
